kind: Service
apiVersion: v1
metadata:
  name: scraper-grpc
  labels:
    app: scraper-grpc
spec:
  selector:
    app: scraper-grpc
    tier: backend
  ports:
  - name: grpc
    port: 3000
    targetPort: 3000
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scraper-grpc
  labels:
    app: scraper-grpc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scraper-grpc
  template:
    metadata:
      labels:
        app: scraper-grpc
    spec:
      containers:
      - name: scraper-grpc
        image: arkhaix/scraper-grpc
        imagePullPolicy: Never # for minikube only. delete for live deployment
        ports:
        - name: grpc
          containerPort: 3000